# assets/icons

UPSI-OS ships all interface icons as inline SVG directly in app.js (see
`App.icon()`) and in the page markup, so no icon files are required here
at runtime. This folder is reserved for any custom icon assets you add
later (e.g. a favicon or app icon) without touching any JS/CSS.
